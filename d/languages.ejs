<% if (LANGUAGES) { %> ## My primary languages. <% for( const {name, value,
color} of LANGUAGES ) { %> <% const val = Math.floor(value*100) %>
<img
  src="https://img.shields.io/badge/<%= name %>-<%= val %>-<%= color.replace('#','') %>?style=for-the-badge"
  alt=""
/>
<% } %> <% } %>
